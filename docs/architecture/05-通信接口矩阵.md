# 通信接口矩阵

说明：表中只列答辩需要的核心接口。频率来自参数或源码；未固定频率的接口写明触发条件。`[可选]` 表示节点、硬件或云连接需要显式启用。

## 交互与语音

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| 操作员 / 本体 QML | `inspection_display_ui_node` | Qt/QML 事件 | 进程内 UI | 系统命令、文本命令、急停、页面操作 | 人工触发 | 急停按钮直接进入 Supervisor 本地通道 | `src/ylhb_llm/qml/`；`inspection_display_ui_node.py` |
| `voice_session_node` | `inspection_agent_node` | `/inspection_ai/agent_request` | ROS Topic `std_msgs/String` | ASR 文本与会话请求 JSON | 本地唤醒并完成 ASR 后 | 本地 KWS 可选；会话有空闲和响应超时 | `llm.yaml`；`voice_session_node.py:43,174`；`inspection_agent_node.py:46,180` |
| `voice_session_node` | DashScope ASR | `https://dashscope.aliyuncs.com/compatible-mode/v1` | HTTPS | 临时 WAV，模型 `qwen3-asr-flash` | 每次有效语音段 | API Key 来自环境变量；请求超时 15 s；本地临时文件清理 | `llm.yaml`；`voice_session_node.py:75-77,534` |
| UI / Agent | `voice_output_node` | `/inspection_ai/say_text` | ROS Topic `ylhb_interfaces/SayText` | 文本、优先级、打断标志 | 有播报结果时 | 可关闭 TTS；支持缓存、分段和打断 | `llm.yaml`；`agent_tools.py`；`voice_output_node.py:20,66` |
| `voice_output_node` | DashScope TTS / 扬声器 | HTTPS + ALSA | HTTPS / 本地音频 | `qwen3-tts-flash` 音频与本地播放 | 收到 `say_text` 后 | 播放超时；可中断当前播放；音频设备由环境变量覆盖 | `llm.yaml`；`voice_output_node.py:26-35,135-179` |
| UI | `voice_session_node` | `/inspection_ai/start_voice_session`、`/inspection_ai/stop_voice_session` | ROS Service `std_srvs/Trigger` | 开启/关闭连续语音会话 | 人工触发 | 关闭语音模式与“结束本轮对话”语义分离 | `llm.yaml`；`voice_session_node.py:49-50,196-204` |
| UI | `voice_input_node` | `/inspection_ai/capture_voice` | ROS Service | 一次性录音识别 | 人工触发 | `[可选]`，不等同连续语音会话 | `llm.yaml`；`voice_input_node.py:21,45` |

## Agent 与工具

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| `inspection_agent_node` | DashScope Planner | `/chat/completions` | HTTPS，OpenAI-compatible | AgentSpec、工具定义、用户请求、工具结果 | 每个 Agent step | `max_agent_steps=12`；API Key 必需；离线安全模式 | `llm.yaml`；`inspection_agent_node.py:70-88`；`inspection_agent_runtime.py` |
| Planner 输出 | Agent Runtime | tool call JSON | 进程内 | 工具名、参数、最终回答、speak | Planner 返回时 | 先过 Agent chat schema，再过 capability schema 与 Policy | `agent_chat_schema.py`；`agent_schema.py`；`agent_policy.py` |
| Agent Runtime | `system_supervisor_node` | `/inspection_ai/system_command` | ROS Topic `std_msgs/String` | 巡逻、检查点、组件控制、急停及关联 ID | 工具获准后 | 禁止工具 `/cmd_vel`、`nav2_goal`、删图、改路线 | `llm.yaml`；`agent_tools.py:_execute_system`；`agent_policy.py:DANGEROUS_TOOLS` |
| Agent Runtime | `base_motion_skill_node` | `/inspection_ai/base_skill_command` | ROS Topic `std_msgs/String` | `rotate_relative`、`move_relative`、`stop_motion` | 工具获准后 | 最大 ±180°、±0.5 m；要求底盘在线和传感器新鲜 | `robot_capabilities.yaml`；`base_motion_skill_node.py:64-78,104` |
| `base_motion_skill_node` / Supervisor | Agent Runtime | `/inspection_ai/base_skill_status`、`/inspection_ai/system_status` | ROS Topic | accepted/running/succeeded/failed/canceled/timeout | 执行状态变化 | 副作用工具进入 `waiting_feedback`；OperationManager 超时后安全取消 | `inspection_agent_node.py:58,181-190,249,485-501`；`agent_operation_manager.py` |
| Agent Runtime | UI / VoiceSession | `/inspection_ai/agent_status`、`agent_event`、`agent_chat` | ROS Topic | Agent 状态、工具事件、最终对话 | 请求与状态变化 | status 使用 latched QoS；真实反馈前不宣称完成 | `llm.yaml`；`inspection_agent_node.py:47-49,126-128` |

## 系统管理

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| UI / Agent / Mobile Bridge | `system_supervisor_node` | `/inspection_ai/system_command` | ROS Topic | start/stop bringup、mapping、navigation、perception、patrol、emergency_stop 等 | 人工或工具触发 | Supervisor 统一进程所有权，避免重复实例 | `llm.yaml`；`system_supervisor_node.py:243,444-449` |
| `system_supervisor_node` | UI / Agent / Mobile Bridge | `/inspection_ai/system_status`、`/inspection_ai/system_mode` | ROS Topic | 组件状态、ready/fault、巡逻准备度、诊断 | 1 Hz 状态定时器及状态变化 | latched QoS；急停将模式置 `fault` | `system_supervisor_node.py:436-439,481,2869-2886` |
| `system_supervisor_node` | Nav2 lifecycle managers | `*/manage_nodes`、`*/get_state` | ROS Service | 启动、查询定位/导航/keepout 生命周期 | 启动巡逻准备链 | 真实 `/amcl_pose`、TF 和 Action server 是最终功能信号 | `system_supervisor_node.py:2241-2338,2636` |
| `system_supervisor_node` | AMCL | `/set_initial_pose` | ROS Service `nav2_msgs/SetInitialPose` | 路线起点初始位姿 | 巡逻启动准备 | 等待新 `/amcl_pose` 和 `map -> odom`，不强制地图原点 | `system_supervisor_node.py:1510-1561` |
| `system_supervisor_node` | RPLidar | `/stop_motor` | ROS Service `std_srvs/Empty` | 停止雷达电机 | 停止 bringup 时 | Service 不可用时记录诊断并继续停止进程 | `system_supervisor_node.py:1055-1070` |
| `system_supervisor_node` | 底盘控制器 | `/cmd_vel` 零 Twist | ROS Topic | 软件急停 | emergency_stop | 连续 5 次、间隔 50 ms；只声明“软件急停” | `system_supervisor_node.py:2869-2881` |

## 巡逻与导航

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| Supervisor / Agent / Mobile Bridge | `patrol_executor` | `/patrol/command` | ROS Topic `std_msgs/String` | start、pause、resume、cancel、takeover、go_to_target、reload、initialize | 高层任务触发 | 只接高层命令；路线从本地已校验 JSON 读取 | `patrol_executor_node.py:616,580-584`；`llm.yaml`；`mobile_bridge.yaml` |
| `patrol_executor` | Supervisor / Agent / Mobile Bridge | `/patrol/status`、`/patrol/event` | ROS Topic `std_msgs/String` | 当前目标、状态、错误、平台关联 ID | 状态变化；状态另以 1 Hz 发布 | 自定义 patrol QoS；终态分 succeeded/failed/canceled | `patrol_executor_node.py:555-563,606` |
| `patrol_executor` | Nav2 | `navigate_to_pose` | ROS Action `nav2_msgs/NavigateToPose` | `map` 坐标目标位姿 | 每个路线目标点 | 等待 Action server；拒绝重试；目标超时取消 goal 并置零 | `patrol_executor_node.py:586-590,939-1143` |
| `patrol_executor` / Mobile Bridge | AMCL | `/initialpose` | ROS Topic `PoseWithCovarianceStamped` | 路线 start_pose 或调试初始位姿 | 初始化/人工触发 | frame=`map`；transient-local QoS | `patrol_executor_node.py:575-579,1162`；`ros_bridge.py:170-174,965-978` |
| Nav2 controller | 底盘控制器 | `/cmd_vel` | ROS Topic `geometry_msgs/Twist` | 线速度、角速度 | 导航控制循环 | 底盘限速；0.5 s 命令看门狗；雷达新鲜度门控 | `zlac8015d.yaml`；`zlac8015d_canopen_controller.cpp:490-498,792` |
| `patrol_executor` | Agent/UI 任务入口 | `/inspection_ai/text_command` | ROS Topic `std_msgs/String` | 到点任务触发文本 | 到达带任务的检查点 | 当前仓库未实现正式设备检测服务/报告数据库 | `patrol_executor_node.py:565-568,597`；README 安全边界 |

## 定位与传感器

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| `rplidar_node` | SLAM Toolbox / AMCL / 底盘安全门控 | `/scan` | ROS Topic `sensor_msgs/LaserScan` | 2D 激光扫描 | 雷达驱动输出，典型约 10 Hz | 串口设备必须存在；底盘要求 0.3 s 内新鲜扫描 | `bringup.launch.py`；`slam_toolbox_params.yaml`；`zlac8015d.yaml` |
| `IMU_publisher` | `ekf_filter_node` | `/imu/data` | ROS Topic `sensor_msgs/Imu` | yaw、yaw rate 等 | IMU 串口数据 | IMU 默认必需；串口权限/设备缺失会阻止 bringup | `bringup.launch.py:241-253`；`ekf.yaml:imu0` |
| 底盘控制器 | `ekf_filter_node` / Nav2 / Mobile Bridge | `/odom` | ROS Topic `nav_msgs/Odometry` | 轮式里程计 | 反馈 30 Hz（ZLAC 配置） | 底盘自身 TF 关闭，避免与 EKF 冲突 | `zlac8015d.yaml`；`base_kinematics.yaml`；`ekf.yaml:odom0` |
| `ekf_filter_node` | 全 ROS 图 | `odom -> base_footprint` | TF | 融合轮速和 IMU 的底盘姿态 | 30 Hz | `world_frame=odom`；统一发布此段 TF | `src/ylhb_base/config/ekf.yaml` |
| SLAM Toolbox 或 AMCL | 全 ROS 图 | `map -> odom` | TF | 建图或定位的全局校正 | SLAM 50 Hz 配置；AMCL 依运动更新 | 建图与定位模式按场景互斥；不由 EKF 发布 | `slam_toolbox_params.yaml`；Nav2 配置 |
| `robot_state_publisher` | 全 ROS 图 | `base_footprint -> base_link -> sensor frames` | TF | URDF 静态/关节坐标关系 | TF 发布 | 传感器 frame 必须与驱动配置一致 | `bringup.launch.py:392-400`；`urdf/ylhb.urdf.xacro` |
| `wtrtk980_nmea_node` | 状态显示/诊断 | `/gps/fix`、`/gps/nmea_sentence`、`/gps/rtk_status` | ROS Topic | GNSS/RTK 定位与诊断 | 读取 20 Hz、状态 1 Hz | `[可选]`；当前不进入 EKF、AMCL、Nav2 | `bringup.launch.py:411-420`；`wtrtk980_nmea_node.cpp:66-73` |

## 感知与三维

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| ZED Wrapper `zed_node` | `yolo_detector_node` | `/zed/zed_node/rgb/color/rect/image` | ROS Topic `sensor_msgs/Image` | 校正 RGB 图像 | 相机帧率 | `[可选]`；默认 TensorRT engine 必须匹配输入尺寸 | `perception.launch.py`；`detector.yaml` |
| `yolo_detector_node` | `object_localizer_node` | `/perception/detections` | ROS Topic `std_msgs/String` | 检测框 JSON | 每个处理帧 | 默认 TensorRT、置信度 0.35、max_det 20 | `detector.yaml`；`yolo_detector_node.cpp:207-211` |
| ZED Wrapper | `object_localizer_node` | depth + camera_info Topics | ROS Topic | 注册深度和相机内参 | 相机帧率 | 只发布 0.2～8 m 有效深度目标 | `perception.launch.py`；`detector.yaml`；`object_localizer_node.py` |
| `object_localizer_node` | Agent / UI | `/perception/localized_objects`、`/perception/target_pose` | ROS Topic | 相机光学坐标系下 3D 目标 JSON / Pose | 检测与深度均有效时 | frame=`zed_left_camera_frame_optical`；未做 map frame 变换 | `detector.yaml`；`object_localizer_node.py:41-46,140-149` |
| Supervisor | `zed_svo_capture_node` | `/inspection_ai/mapping3d_capture_command` | ROS Topic `std_msgs/String` | SVO 采集 start/stop 命令 | UI/系统命令触发 | `[可选]`；这是 Supervisor 当前正式双阶段采集入口，ZED SDK 独占 | `system_supervisor_node.py:413,443`；`zed_svo_capture_node.py:25-37` |
| 外部命令发布者 | `zed_spatial_mapping_node` | `/inspection_ai/mapping3d_command` | ROS Topic `std_msgs/String` | 直接 Spatial Mapping start/stop/export | 使用专用 Launch 时触发 | `[可选]`；与 ZED Wrapper/其他 SDK 客户端互斥 | `zed_spatial_mapping.yaml`；`zed_spatial_mapping_node.py:164-224` |
| `zed_svo_capture_node` / `zed_spatial_mapping_node` | Supervisor / UI | `/inspection_ai/mapping3d_status`、`/inspection_ai/mapping3d_result`；Spatial Mapping 另有 `/inspection_ai/mapping3d_pointcloud` | ROS Topic | 状态、导出结果、预览点云 | 状态变化；预览默认 1 Hz | SVO/PLY/OBJ 与预览点云不写 Nav2 `my_map.*` | `zed_svo_capture_node.py:30-37`；`zed_spatial_mapping.yaml`；`zed_spatial_mapping_node.py:221-224` |

## 手机与 Mobile Bridge

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| 手机调试端 | `mobile_bridge_server` | `/api/status`、`/api/debug/*`、`/api/cmd_vel`、`/api/text_command` | HTTP `:8000` | 状态、建图、地图、巡逻、导航与受限调试控制 | REST 请求 | 仅可信局域网；可启用 token；速度限幅 0.30 m/s、0.55 rad/s | `mobile_bridge.yaml`；`mobile_bridge_server.py:265-621` |
| `mobile_bridge_server` | 手机调试端 | `/ws/status`、`/ws/map` | WebSocket | 机器人状态、PNG 地图快照 | 状态默认 2 Hz；地图默认 1 Hz | 服务端单向推送；断开/异常发布零速度；token 错误关闭码 1008 | `mobile_bridge.yaml`；`mobile_bridge_server.py:625-708` |
| 手机调试端 | `MapManager` | `POST /api/debug/maps/{name}/confirm_default` | 本地 HTTP / 函数调用 | 确认默认地图 | 人工确认 | map_server/SLAM 运行时拒绝；归档旧地图与旧路线；本地成功与云上传解耦 | `mobile_bridge_server.py:358-397`；`map_manager.py:304-407` |
| `mobile_bridge` | ROS 图 | `/odom`、`/scan`、`/map`、`/imu/data`、`/amcl_pose`、底盘/系统/巡逻状态 | ROS Topic 订阅 | 聚合状态与地图 | Topic 更新 | sensor QoS / map transient-local QoS；仅桥接，不改变接口类型 | `ros_bridge.py:179-239`；`mobile_bridge.yaml` |
| 运维/UI | `mobile_bridge` | `/mobile_bridge/set_cloud_enabled`、`set_local_app_enabled` | ROS Service `std_srvs/SetBool` | 云连接和本地 APP 服务开关 | 人工触发 | 状态分别发布到 cloud/local_app status Topics | `mobile_bridge.yaml`；`ros_bridge.py:175-178,275-278` |

## Jetson 与云平台

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| `PlatformCloudClient` | Robot Bridge | `POST /robot-api/v1/heartbeat` | 公网 HTTPS | 状态、活动 execution/deployment、事件游标、map/odom pose、健康度 | 空闲默认 3 s；有命令默认 1 s | Jetson 主动发起；Bearer Robot Token；TLS 校验，不接受公网入站控制 | `platform_cloud_client.py:49-58,212-223,278-288`；协议 6.1 |
| `PlatformCloudClient` | Robot Bridge | command ACK / events batch | 公网 HTTPS | 命令持久化 ACK、最多 100 条事件 | heartbeat 后或有积压事件 | ACK 只表示已持久化，不表示 ROS 已执行；事件按 sequence 去重补传 | `platform_cloud_client.py:203-210,254-285`；协议 6.2/6.3 |
| `MapUploadWorker` | Robot Bridge / Spring 地图资产 | `POST /robot-api/v1/map-assets` | 公网 HTTPS multipart | YAML、PGM、归一化身份、SHA-256、幂等键 | `confirm_default` 后异步 | 只允许 HTTPS；大小限制；指数退避；成功后平台状态仍为 `PENDING_REVIEW` | `map_upload.py:122-139,317-386`；协议 6.4 |
| `PlatformCloudClient` | Robot Bridge | Deployment manifest/route/yaml/pgm GET | 公网 HTTPS | 不可变部署四件套 | 收到 START 且本地未安装 deployment | 校验 robot/deployment 身份、路线双哈希、地图哈希和 YAML-PGM 绑定 | `platform_cloud_client.py:225-240`；`platform_store.py:159-241`；协议 6.5～6.8 |
| `DeploymentStore` | 本地文件系统 / SQLite | `platform.db`、`deployments/`、`staging/` | SQLite / 文件 | deployment、execution、命令、事件、上传任务 | 命令、事件、上传、安装时 | staging 后 `os.replace` 原子安装；deploymentId 内容冲突返回 409 | `platform_store.py:67-115,159-257` |
| Spring / Web | Robot Bridge | `/bridge/v1/*`、Spring `/api/v1/*` | HTTPS / 平台内部接口 | 地图审核、路线修订、任务与控制 | 平台业务触发 | **外部系统/协议已定义，源码不在本仓库**；浏览器不直连 Jetson | `docs/protocol/robot-platform-v1.md:17-31,352-488,609-649` |

## 硬件通信

| 发送方 | 接收方 | 接口或 Topic | 通信类型 | 数据内容 | 频率或触发条件 | 可靠性/安全约束 | 对应源码或配置 |
|---|---|---|---|---|---|---|---|
| `zlac8015d_canopen_controller` | ZLAC8015D | SocketCAN `can1` / CANopen SDO | CANopen | 控制字、目标速度、实际速度、故障 | 控制 50 Hz、反馈 30 Hz、CAN 轮询 200 Hz、故障 2 Hz | 当前 `use_pdo=false`，PDO 仅保留后续；退出停车；看门狗置零 | `zlac8015d.yaml`；`zlac8015d_canopen_controller.cpp` |
| `base_controller` | STM32 底盘 | `/dev/ttyS1` | 串口 | 左右轮速度帧与反馈 | 定时控制/读取 | `[可选回退]`；与 ZLAC 后端互斥；同样不发布 TF | `bringup.launch.py:372-389`；`base_controller.cpp` |
| `rplidar_node` | RPLidar A2M8 | `/dev/robot_lidar`，CP2102 | USB 串口 | 雷达扫描 | 115200 baud | 设备别名缺失时探测 `/dev/ttyUSB*`；关键节点异常退出会关闭 bringup | `bringup.launch.py:264-279,324-335` |
| `IMU_publisher` | N300WP PRO / HiPNUC IMU | `/dev/robot_imu` | USB 串口 | IMU 原始帧 | 115200 baud | 默认必需；USB ID/权限/驱动严格检查 | `bringup.launch.py:20-253`；`hipnuc_imu` |
| `wtrtk980_nmea_node` | WTRTK980 RTK | `/dev/rtk_4g` | USB 串口 NMEA | GGA/NMEA 与 RTK 状态 | 115200 baud，读取 20 Hz | `[可选]`；第一阶段只做数据验证和显示 | `bringup.launch.py:336-355,411-420`；`wtrtk980_nmea_node.cpp` |
| ZED Wrapper / Spatial Mapping | ZED 2i | USB + ZED SDK | USB / SDK | RGB、深度、相机位姿、SVO、点云/网格 | 配置 15 FPS（3D）或 wrapper 参数 | `[可选]`；单相机通常只能被一个 ZED SDK 客户端占用 | `perception.launch.py`；`zed_spatial_mapping.launch.py`；三维文档 |
| Voice nodes | 麦克风 / 扬声器 | ALSA (`arecord`/播放器) | 本地音频 | 16 kHz 录音、提示音、TTS 音频 | 会话监听/播报时 | `[可选]`；输入输出设备可由 `YLHB_AUDIO_*` 环境变量覆盖 | `llm.launch.py`；`llm.yaml`；`voice_session_node.py`；`voice_output_node.py` |
