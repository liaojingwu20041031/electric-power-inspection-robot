# 快速使用

## 当前使用前提

当前仓库用于 Jetson/ROS 2 Humble 实机联调，也可在 PC 端做文档、Python 逻辑和
部分 ROS 包构建检查。完整巡检业务闭环仍在扩展中，实机运行前应先阅读主调试手册。

## PC 端仓库初始化流程

推荐统一使用 `~/ros2_DL`：

```bash
cd ~
git clone https://github.com/liaojingwu20041031/electric-power-inspection-robot.git ros2_DL
cd ~/ros2_DL
```

## 可选编译检查

如果本机已安装 ROS 2 Humble：

```bash
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source ~/ros2_DL/install/setup.bash
```

如果当前 PC 没有 ROS 2 环境，可以先跳过 colcon 构建，只做文档和 Python 语法检查。

## 可选 LLM 框架启动检查

```bash
ros2 launch ylhb_llm llm.launch.py enable_voice:=false enable_tts:=false
```

另开终端发送文本任务：

```bash
ros2 topic pub --once /inspection_ai/text_command std_msgs/msg/String \
  "{data: '{\"source\":\"cli\",\"text\":\"开始巡检任务\"}'}"
```

查看输出：

```bash
ros2 topic echo /inspection_ai/task_event
ros2 topic echo /inspection_ai/say_text
```

## 后续开发板部署入口

开发板验证时使用：

```bash
cd ~/ros2_DL
./scripts/install_jetson_dependencies.sh
./scripts/build_on_jetson.sh
./scripts/run_on_jetson.sh bringup
./scripts/run_on_jetson.sh mapping
./scripts/run_on_jetson.sh navigation
./scripts/run_on_jetson.sh inspection
```

本地巡逻路线统一写入 `~/ros2_DL/maps/route_patrol_*.json`。底层和 Nav2 已启动后，
启动巡逻执行器：

```bash
ros2 launch ylhb_mobile_bridge patrol_executor.launch.py \
  auto_start:=false \
  publish_initial_pose_on_startup:=true
```

`auto_start:=false` 表示只加载路线并发布初始位姿，随后停在 `idle` 等待人工确认。
确认定位稳定、现场安全后，再启动巡逻：

```bash
ros2 topic pub --once /patrol/command std_msgs/msg/String "{data: start}"
```

路线格式、状态观察、暂停/恢复/取消和验收步骤见
[重点使用调试文档](../src/PROJECT_DOC_zh.md#12-本地巡逻-patrol-调试)。

## 下一步阅读

详细调试步骤、CAN、bringup、mapping、navigation、perception、LLM 和常见问题请阅读 [重点使用调试文档](../src/PROJECT_DOC_zh.md)。
